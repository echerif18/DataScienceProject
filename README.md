# Data_science_lab

In order to run "ComE", navigate to its folder and install dependencies recursively that given in the file named "requirements.txt". You can easily do that by executing "pip install -r requirements.txt" inside of your virtual environment and it should work appropriately. So far, I only tried on my system (Windows), it may (or may not) create some issues on other operating systems due to dependencies and etc.
